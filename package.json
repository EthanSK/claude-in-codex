{
  "name": "codex-claude-bridge",
  "version": "0.1.0",
  "description": "Use Claude (via your local Claude Code CLI) from the Codex app's model picker, next to GPT.",
  "type": "module",
  "private": true,
  "bin": {
    "codex-claude-bridge": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "ws": "^8.21.3"
  }
}
